# Create React App only exposes variables prefixed with REACT_APP_
# Stripe publishable keys (pk_...) are safe to use in the frontend.
# Use pk_test_... locally/staging and pk_live_... in production.
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
